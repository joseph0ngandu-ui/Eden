# Eden Repository Default Configuration
# This is the canonical default configuration for all Eden operations
# Used by backtests, trading, and other system components unless overridden

extends: ./profiles/current.yaml
profile: rapid-optimized

# Override settings can be added here if needed for global defaults
# The extends directive will load the base configuration first

# Version and metadata
config_version: "1.0.0"
default_profile: "rapid-optimized"
created_at: "2025-10-22T17:02:00Z"
description: "Repository default configuration using rapid-optimized profile"